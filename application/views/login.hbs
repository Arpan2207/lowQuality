<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{pageTitle}}</title>
    <link rel="stylesheet" href="../public/css/login.css">
</head>
<body>
    <header>
        <h1>{{headerTitle}}</h1>
        <h2>{{formTitle}}</h2>
    </header>

    <main>
        <p>{{instruction}}</p>
        <section>
            <input type="text" placeholder="{{usernamePlaceholder}}" name="username" required><br><br>
            <input type="password" placeholder="{{passwordPlaceholder}}" name="password" required><br><br>  
            <button type="submit">{{buttonText}}</button>
        </section>
            
        <br>
        <br>
        <pre>
            {{#each buttons}}
                <button class="button"><a href="{{this.href}}">{{this.text}}</a></button>
            {{/each}}
        </pre>
    </main>

    <footer>
        <h4>{{footerText}}</h4>
    </footer>
</body>
</html>
